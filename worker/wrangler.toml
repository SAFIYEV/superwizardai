name = "superwizard-api"
main = "worker.js"
compatibility_date = "2024-12-01"

# After first deploy, run:
# npx wrangler secret put NVIDIA_API_KEY
# Then paste your NVIDIA API key — it will be stored encrypted in Cloudflare
